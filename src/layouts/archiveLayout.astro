---
import BaseLayout from './BaseLayout.astro';

export interface Props {
    title: string;
    description?: string;
    image?: string;
}

const { title, description, image } = Astro.props;
---

<BaseLayout
        title={title}
        description={description}
        image={image || '/default-og-image.png'}
>
    <div class="archive-container">
        <main class="archive-body">
            <slot />
        </main>

        <footer class="resource-disclaimer">
            <p>
                여기서 제공되는 자료는 비상업적 용도의 아카이빙 자료입니다.
            </p>
            <p>
                본 사이트는 자료의 저작권 소유 기관 및 관계자와 어떠한 특수한 관계도 없음을 밝힙니다.
            </p>
            <p>
                원문 페이지 이동을 우선으로 하며, 원문 사이트 접속 불가 시에만 다운로드 링크를 제공합니다.
            </p>
        </footer>
    </div>
</BaseLayout>

<style is:global>

    .resource-disclaimer {
        background-color: #f8f8f8;
        border: 1px solid #eee;
        padding: 1rem;
        margin-top: 2rem;
        margin-bottom: 2rem;
        border-radius: 4px;
        font-size: 0.9rem;
        color: #555;
        line-height: 1.6;
        text-align: center;
        /* archive-container의 영향을 받으므로 max-width와 margin-left/right는 필요 없습니다. */
    }

    .resource-disclaimer p {
        margin: 0;
    }
</style>