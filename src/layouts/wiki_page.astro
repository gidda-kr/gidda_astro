---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <style is:global>
    /* 전체 페이지에 적용되는 스타일 */
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
      background-color: #f9f9f9;
      color: #333;
    }
    /* 메인 콘텐츠 영역에 패딩과 최대 너비를 줍니다. */
    main {
      max-width: 800px;
      margin: 2rem auto; /* 중앙 정렬 */
      padding: 1rem 2rem; /* 내부 여백 */
    }
    /* 제목과 문단에 여백을 추가합니다. */
    main p, main ul, main ol {
      margin-bottom: 1.5rem;
    }

    /* --- [변경 1] h1, h2 폰트 크기 조정 --- */
    main h1 {
      font-size: 2.5rem; /* 제목(h1)은 크게 */
      border-bottom: 2px solid #ccc;
      padding-bottom: 0.5rem;
      margin-bottom: 2rem;
    }
    main h2 {
      font-size: 1.8rem; /* 부제목(h2)은 h1보다 작게 */
      margin-top: 3rem;
      margin-bottom: 1.5rem;
    }

    /* --- [변경 2] 각주 폰트 크기 조정 --- */
    /* Remarkable 라이브러리가 생성하는 각주 영역에 스타일을 적용합니다. */
    .footnote-backref {
      font-size: 0.75rem; /* 각주 번호 글자 크기 */
    }
    .footnotes {
      margin-top: 3rem;
      padding-top: 1.5rem;
      border-top: 1px solid #ddd;
      font-size: 0.9rem; /* 각주 본문 글자 크기 */
    }
    .footnotes p {
      font-size: 0.9rem; /* 각주 본문 문단 글자 크기 */
      margin-bottom: 0.5rem;
    }
  </style>
</head>
<body>
<header style="text-align:center; padding:1rem;">
  <a href="/wiki" style="text-decoration:none; color:#007bff; font-weight:bold;">← 위키 대문으로 돌아가기</a>
</header>

<main>
  <slot /> {/* 마크다운 콘텐츠가 이 안에 들어갑니다. */}
</main>
</body>
</html>